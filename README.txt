Client and Server for a local network chat application. Application allows for message broadcasting to other clients, as individuals or as groups.

Connection between Client and Server is UDP, however TCP protocols have been built on top of the underlying connection as an exploration of how TCP and UDP work.

HOW TO USE:

1. Launch Server.py 

2. Lauch Client.py

	Follow on screen prompts
